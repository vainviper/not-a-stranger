<% include ./partials/header %>

<div class="container">
    <div class="jumbotron">
        <h1 class="animatedTitle">NOT A STRANGER</h1>
        <% if(currentUser) { %>
            <p class= "text-muted">Go To <a class="hoverTextDefault" href="/login">Collections</a></p>
        <% } else { %>
            <p class="text-muted"><a class="hoverTextDefault" href="/login">Sign In</a> Or <a class="hoverTextDefault" href="/register">Register</a> To Continue</p>
        <% }%>
    </div>
</div>

<% include ./partials/footer %>